import React from 'react';

export default function App() {
  return (
    <div className="app-shell">
      <header className="app-header">
        <h1>Frontend Client (React + Vite)</h1>
        <p>Starter scaffold is ready.</p>
      </header>
      <main>
        <div className="card">
          <h2>Next Steps</h2>
          <ul>
            <li>Edit <code>src/App.jsx</code> to build your UI.</li>
            <li>Add components under <code>src/</code>.</li>
            <li>Run <code>npm install</code> then <code>npm run dev</code>.</li>
          </ul>
        </div>
      </main>
    </div>
  );
}

